#!/usr/bin/env bash
# Start with checking so git and curl are installed. If they are not then install them.

# Helpers for package managers

# Check distribution

# Install software

# Install MyPrompt
git clone --depth=1 https://github.com/fredhawk/myprompt.git && cd myprompt && bash ./setup.sh

# Finish
